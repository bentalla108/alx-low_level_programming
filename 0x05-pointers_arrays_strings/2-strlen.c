#include "main.h"
#include <stdio.h>
#include <string.h>
/**
*_strlen - Take char and calcul his len
*
*@s : Parameter to calcul len
*
*Return: Return the len of s
*@s : the value to return
*/

int _strlen(char *s)
{

	return (strlen(s));
}
